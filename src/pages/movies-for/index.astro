---
import Layout from '../../layouts/Layout.astro';
import { OCCASIONS } from '../../lib/supabase';

const siteUrl = 'https://goodwatch.movie';

const schema = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": "Movies for Every Occasion",
  "description": "Find the perfect movie for any occasion - date night, rainy day, family time, and more.",
  "url": `${siteUrl}/movies-for`
};
---

<Layout 
  title="Movies for Every Occasion | Date Night, Rainy Day & More"
  description="Find the perfect movie for any occasion. Browse curated recommendations for date night, rainy days, family movie night, and more. 100+ movies for every mood."
  schema={schema}
>
  <div class="max-w-7xl mx-auto px-4 py-12">
    <!-- Breadcrumb -->
    <nav class="text-sm text-gw-text-secondary mb-6">
      <a href="/" class="hover:text-white">Home</a>
      <span class="mx-2">›</span>
      <span class="text-white">Movies For</span>
    </nav>

    <h1 class="text-4xl md:text-5xl font-bold mb-4">Movies for Every Occasion</h1>
    <p class="text-xl text-gw-text-secondary mb-12 max-w-3xl">
      Not sure what to watch? Browse our curated collections for specific occasions and moods.
      Each list is hand-picked to match the moment perfectly.
    </p>

    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {OCCASIONS.map(occasion => (
        <a 
          href={`/movies-for/${occasion.slug}`}
          class="bg-gw-card hover:bg-gw-border border border-gw-border rounded-xl p-6 transition group"
        >
          <h2 class="text-xl font-bold mb-2 group-hover:text-gw-accent transition">
            {occasion.name}
          </h2>
          <p class="text-gw-text-secondary text-sm mb-4">
            {occasion.description}
          </p>
          <span class="text-gw-accent text-sm font-medium">
            Browse movies →
          </span>
        </a>
      ))}
    </div>

    <!-- SEO Content -->
    <section class="border-t border-gw-border mt-16 pt-12">
      <h2 class="text-2xl font-bold mb-6">Find Movies for Any Moment</h2>
      <div class="prose prose-invert max-w-none">
        <p class="text-gw-text-secondary mb-4">
          Every occasion calls for a different type of movie. A cozy rainy day deserves something different 
          than an exciting date night. GoodWatch helps you find the perfect film for whatever moment you're in.
        </p>
        <p class="text-gw-text-secondary">
          Our occasion-based recommendations go beyond simple genre categories. We consider mood, pacing, 
          and emotional tone to suggest movies that truly fit the vibe you're looking for.
        </p>
      </div>
    </section>
  </div>
</Layout>
